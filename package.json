{
  "name": "final-project-jcwd-2102-01",
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream",
    "dev": "lerna run dev --stream",
    "clean": "lerna clean --y",
    "test": "lerna run test",
    "install:server": "lerna add --scope=server",
    "install:client": "lerna add --scope=client",
    "install:server:dev": "lerna add --scope=server --dev",
    "install:client:dev": "lerna add --scope=client --dev",
    "server": "lerna exec --scope server npm run",
    "client": "lerna exec --scope client npm run"
  },
  "private": true,
  "devDependencies": {
    "lerna": "^5.4.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
